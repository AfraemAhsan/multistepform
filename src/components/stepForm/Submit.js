import React from "react";
import Container from "@material-ui/core/Container";
const Submit = () => {
  return (
    <Container maxWidth="sm">
      <h2>Thank you for subitting we will be in touch</h2>
    </Container>
  );
};

export default Submit;
